require 'rubygems'
require 'rake'
require 'echoe'

Echoe.new('gappster', '0.0.1') do |p|
  p.description    = "Google Apps authentication"
  p.url            = "http://github.com/crowdint/gappster"
  p.author         = "David Padilla"
  p.email          = "david@crowdint.com"
  p.ignore_pattern = ["tmp/*", "script/*"]
  p.development_dependencies = []
end
